{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.heimgewebe.org/contracts/heimlern.ingest.state.schema.json",
  "title": "Heimlern Ingest State",
  "type": "object",
  "properties": {
    "cursor": {
      "type": "string",
      "description": "The identifier (timestamp or ID) of the last successfully processed event."
    },
    "last_ok": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the ingest process last ran successfully."
    },
    "last_error": {
      "type": ["string", "null"],
      "description": "Error message if the last run failed."
    }
  },
  "required": ["cursor", "last_ok"]
}
